mdadm --examine --scan >> /etc/mdadm/mdadm.conf

remember that  /etc/mdadm/mdadm.conf in modern OSes may become /etc/mdadm.conf
just check it


The output of this command contains the uuids of your devices and copies them to your mdadm.conf (assuming it’s in that location). 
