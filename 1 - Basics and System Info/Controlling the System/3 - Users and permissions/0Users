add a new user

sudo adduser newuser2

remember you can also use an user with a dot inside like: 'mark.spencer'

to remove a user

sudo userdel newuser2



to edit the name of a user:

sudo usermod -l newuser3 newuser2



to change the password for a user:

sudo passwd newuser2



To edit the username of a user:
sudo usermod -l new_username old_username
