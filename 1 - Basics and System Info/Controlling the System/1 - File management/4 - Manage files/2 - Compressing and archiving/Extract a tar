tar -xJf myarchive.tar.xz 


you can add or omit the 'v' option for verbose mode 


Note that by default, if an extracted file already exists, tar
will remove the existing file and replace it with the extracted file.


you can check other ooptions with

man tar

---
if you want to see the content without extracting just use the 't' option:

tar -tvf myarchive.tar