
You can check rules with:

sudo firewall-cmd --list-all-

with firewalld (in RHEL and derivatives):

to add a new rule:


sudo firewall-cmd --permanent --add-port=3389/tcp
sudo firewall-cmd --reload

you must indicate the protocol 'tcp' or 'udp'


with ufw just run:

sudo ufw allow 3389
