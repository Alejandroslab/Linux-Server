sudo apt install python3-pip

Print the pip3 version to verify the installation:

pip3 --version


if you have python 2 version :

sudo apt install python-pip


it is better to install pip for python 3 as this is the last version

to install just use

pip3 install namepackage
