To make a bash script executable

First create the script with extention '.sh'

nano myscript.sh


then make it exectuable with:

sudo chmod +x myscript.sh

finally you can run the script:
./myscript.sh
