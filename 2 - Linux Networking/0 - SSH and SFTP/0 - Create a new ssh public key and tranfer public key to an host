first check if you have already a public key:

cat ~/.ssh/id_rsa.pub

if you do not have one just create with the command:

ssh-keygen -b 4096



finally to transfer this new public key just run:

ssh-copy-id -i ~/.ssh/id_rsa.pub YOUR_USER_NAME@IP_ADDRESS_OF_THE_SERVER





